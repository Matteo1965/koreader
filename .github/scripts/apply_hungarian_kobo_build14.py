# Superseded by apply_hungarian_kobo_build15.py.
#
# Kobo build #14 failed before compilation because this patch used a brittle
# insertion marker that did not match the actual post-Build-3 CREngine source.
# The corrected Build #15 patch anchors on huAddExtendedHyphenation() instead.

raise SystemExit("Superseded by apply_hungarian_kobo_build15.py")
